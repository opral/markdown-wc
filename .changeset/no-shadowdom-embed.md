---
"@opral/markdown-wc": patch
---

Render `<markdown-wc-embed>` in light DOM so host page styles apply by default.
Refresh `default.css` to match inlang website markdown styling and scope it to `.markdown-body`.
